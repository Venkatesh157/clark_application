import {StyleSheet} from 'react-native';

const useStyles = () =>
  StyleSheet.create({
    input: {
      height: 40,
      margin: 12,
      borderWidth: 1,
      padding: 10,
      borderRadius: 5,
    },
  });

export default useStyles;
